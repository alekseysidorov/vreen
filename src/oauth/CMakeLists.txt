set(QT_USE_QTWEBKIT true)
add_simple_library(vkitoauth
    STATIC CXX11
    DEFINE_SYMBOL VK_LIBRARY
    VERSION ${CMAKE_LIBVK_VERSION_STRING}
    SOVERSION ${CMAKE_LIBVK_VERSION_MAJOR}
	LIBRARIES vkit ${QT_QTWEBKIT_LIBRARY} ${K8JSON_LIBRARIES}
	INCLUDES ${PROJECT_BINARY_DIR}/include ${QT_QTWEBKIT_INCLUDE_DIR} ${K8JSON_INCLUDE_DIRS}
    INCLUDE_DIR vk/auth
)
add_pkgconfig_file(vkitoauth.pc.cmake)
