if(NOT VKIT_INSTALL_HEADERS)
    set(INTERNAL_FLAG "INTERNAL")
endif()

add_subdirectory(3rdparty)
add_subdirectory(api)
if(VKIT_WITH_QMLAPI)
    add_subdirectory(qml)
endif()
if(VKIT_WITH_OAUTH)
    add_subdirectory(oauth)
endif()
